// Reverse a string
//steps;
//measure string length
//create a loop that counts until length is reached
//set index value to length of string and count backwards

function reverse(word) {
  var index = 0;
  for (var i = word.length; i >= index; i--) {
     console.log(word[i]);
  }
  return console.log(word[i]);
}

var reverse_name = reverse("Youssef");

if (1==1) {
	console.log("here is your reversed string" + reverse_name);
}
